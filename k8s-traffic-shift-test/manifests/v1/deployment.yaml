apiVersion: apps/v1
kind: Deployment
metadata:
  name: trafficshift-v1
  labels:
    app: trafficshift
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trafficshift
      version: v1
  template:
    metadata:
      labels:
        app: trafficshift
        version: v1
    spec:
      containers:
        - name: trafficshift
          image: hashicorp/http-echo:latest
          args:
            - -text=v1
            - -listen=:5000
          ports:
            - containerPort: 5000
